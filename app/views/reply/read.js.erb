//document.getElementById("replies").remove();
//console.log(document.getElementsByClassName("reply").length);
//for (let i = 0; i < document.getElementsByClassName("reply").length; i++) {
//    document.getElementsByClassName("reply").item(0).remove();
//    console.log(i);
//}
while (document.getElementsByClassName("reply").length > 0) {
    document.getElementsByClassName("reply").item(0).remove();
}
//while (document.querySelectorAll("form").length > 0) {
//    document.querySelector("form").remove();
//}
while (document.getElementsByClassName("replyAction").length > 0) {
    document.getElementsByClassName("replyAction").item(0).remove();
}

document.getElementById("replies").innerHTML +=
    "<%= escape_javascript render 'replies' %>"
